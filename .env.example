# Copy to .env.local for local development.
# In production on Vercel, add the Postgres storage integration
# (Project → Storage → Create Database → Postgres) and Vercel
# injects POSTGRES_URL automatically — no manual setup needed.
POSTGRES_URL=postgresql://user:password@localhost:5432/life_hacks
